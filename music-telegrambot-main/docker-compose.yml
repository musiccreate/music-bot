version: '3.8'

services:
  music-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: music-telegram-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - VK_ACCESS_TOKEN=${VK_ACCESS_TOKEN}
      - YANDEX_TOKEN=${YANDEX_TOKEN}
      - WX_ACCESS_TOKEN=${WX_ACCESS_TOKEN}
    volumes:
      - /tmp/music_bot:/tmp/music_bot
      - ./logs:/app/logs
    networks:
      - music-bot-network

networks:
  music-bot-network:
    driver: bridge
